export interface Project{
  proyectoId?: number,
  nombre?: string,
  presupuesto?: number,
  estaDisponible?: boolean,
  fechaCreacion?: Date,
  ciudad?: string,
  propietario?: string,
  cliente?: string,
}
